1,userProfile.vue 分析其行为，在登录及非登录的状态下进行点击触发不同的方法，
  先测试界面，后测试行为



2, 测试登录，登出，难点，有很多全局的组件，如 a-button，a-dropdown-button，组件中并没有这直接导入
   在mount 方法里添加一个global属性，用来转化一些全局方面的内容
   查看 userProfile.spec.ts 文件


3,  查看这两个文件
userProfile.spec.ts
userProfile2.spec.ts



4，单独测试vuex 的store
   查看store.spec.ts 文件

    测试过程
        1,检查初始 state 是否正常
        2,触发 mutations 或者 actions，对于每个 mutations 可以写一个 case
        3,检查修改后的 state 是否正常
        4,测试 getters



在jest.config.js 里面添加

  transformIgnorePatterns: [
    "<rootDir>/node_modules/(?!lodash-es)"
  ],

这样jest 就可以解析lodash-es了，