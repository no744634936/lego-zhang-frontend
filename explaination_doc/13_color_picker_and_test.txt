1, colorPicker.vue
   渲染一个input 颜色选标签，在它的右边将defaultColors里的常用颜色选项渲染出来，

   这个input颜色选择器有点特殊，点击了上面的颜色之后，都会自动触发元素上的input事件
   input事件又会触发onChange事件，onchange事件将，input变化后的值$event.target.value
   发送给store

   点击常用颜色选项也可以触发触发onChange事件，onchange事件将选中的颜色发送给store

2，写测试，查看 colorPicker.ts 文件