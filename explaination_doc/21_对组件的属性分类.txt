1,组件属性分类两大类

{
    //业务组件的特有属性
    ....

    //通用属性
    common_props_with_default_value
}


2,组件的通用属性分为很多小类
{
    // actions
    // size
    // border type
    // shadow and opacity
    // position and x,y
    // position: 'absolute',
}

3,创建一个新的组件 EditGroup

<EditGroup :props="currentElement.props"></EditGroup>

EditGroup的目的就是将currentElement的props转换成一个object数组，
数组里的每一个object对应一个选项卡

[
    {
        text:'基础属性',
        item:[....]
    },
    {
        text:'尺寸',
        item:[....]
    },
    {
        text:'位置',
        item:[....]
    },
]


------------------------------------
1，前期的数据准备

<1>
editor.ts 文件里面混入 textDefaultProps, imageDefaultProps
import { textDefaultProps, imageDefaultProps } from 'lego-bricks'

<2>
mapPropsToForms.tsx 文件里添加了很多属性对应的组件

<3>
建立ShadowPicker.vue 文件

<4>
建立 EditGroup.vue 文件

<5>
EditGroup.vue 文件 导入 Editor.vue 文件

<6>
